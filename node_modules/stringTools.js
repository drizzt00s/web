function cutLocation(s){
	if((typeof s)!="string"){
       throw new Error("参数不是字符串!");
	}
    var sToArray=s.split(",");
    return {
    	provinceLocation:sToArray[0],
    	cityLocation:sToArray[1],
    	subCityLocation:sToArray[2]
    };
}
exports.cutLocation=cutLocation;